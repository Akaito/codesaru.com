<?xml version="1.0" ?>
<UpgradeInfo version="1">
    <Boolean name="UpgradeAvailable" value="{{ upgrade_available }}"/>
    <String name="Description">{{ description }}</String>
    <String name="BaseVersion">{{ base_version }}</String>
    <String name="TargetVersion">{{ target_version }}</String>
    {% if motd %}
    <String name="motd">{{ motd }}</String>
    {% endif %}
    {% for link in links %}
        <String name="link{{ loop.index }}">{{ link.url }}</String>
        {% if link.name %}
            <String name="link{{ loop.index }}Name">{{ link.name }}</String>
        {% endif %}
    {% endfor %}
</UpgradeInfo>

